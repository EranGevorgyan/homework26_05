for number in range(1, 10000):
    result = 0
    x = 1
    while x <= (number // 2):
        if number % x == 0:
            result = result + x
        x = x + 1
    if result == number:
        print(f"{number} is a perfect number.")
